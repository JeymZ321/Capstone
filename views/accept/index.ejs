<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, shrink-to-fit=no">
    <title>Approved Requests - Appointment</title>
    <link rel="icon" type="image/png" sizes="32x32" href="/assets/img/reynaldoslogo.png">
    <link rel="stylesheet" href="/admin/assets/bootstrap/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Nunito:200,200i,300,300i,400,400i,600,600i,700,700i,800,800i,900,900i">
    <link rel="stylesheet" href="/admin/assets/fonts/fontawesome-all.min.css">
    <link rel="stylesheet" href="/admin/assets/fonts/font-awesome.min.css">
    <link rel="stylesheet" href="/admin/assets/fonts/fontawesome5-overrides.min.css">
    <link rel="stylesheet" href="/admin/assets/css/admin.css">
    <link rel="stylesheet" href="/admin/assets/css/untitled.css">
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

    <style>
        input[type="text"],
        input[type="email"],
        input[type="password"]{
            width: 100%;
            text-align: left; 
            border: 1px solid #ccc;
            border-radius: 10px;
            box-sizing: border-box;
            padding: 5px;
            margin: 5px;
        }

        .image-preview {
            width: 200px; /* Set fixed width */
            height: 200px; /* Set fixed height */
            border: 1px solid #ccc; /* Optional: add a border */
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden; /* Ensure content does not overflow */
        }
        .image-preview img {
            max-width: 100%; /* Ensure the image fits within the container */
            max-height: 100%; /* Ensure the image fits within the container */
            object-fit: cover; /* Maintain aspect ratio */
        }

        label{
            color: black;
            font-weight: bold;
        }

     

    </style>
</head>

<body id="page-top">
    <div id="wrapper">
        <nav class="navbar navbar-dark align-items-start sidebar sidebar-dark accordion bg-gradient-primary p-0" style="background: rgb(0,0,0);max-width: none;width: 120px;">
            <div class="container-fluid d-flex flex-column p-0">
                <a class="navbar-brand d-flex justify-content-center align-items-center sidebar-brand m-0" href="#" style="background: url('/assets/img/reynaldoslogo.png') no-repeat;background-size: contain;border-radius: 40px;width: 120px;height: 120px;">
                    <div class="sidebar-brand-icon rotate-n-15"></div>
                    <div class="sidebar-brand-text mx-3"></div>
                </a>
                <hr class="sidebar-divider my-0">
                <ul class="navbar-nav text-light" id="accordionSidebar">
                    <li class="nav-item" id="dashboard-item">
                        <a class="nav-link" href="/admin/admindashboard.html">
                            <i class="fas fa-tachometer-alt"></i>
                            <span>Dashboard</span>
                        </a>
                    </li>
                    <li class="nav-item" id="calendar-item">
                        <a class="nav-link" href="/admin/calendar.html">
                            <i class="fa fa-calendar"></i>
                            <span>Calendar</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="#" data-bs-toggle="collapse" data-bs-target="#appointmentDropdown" aria-expanded="false" aria-controls="appointmentDropdown">
                            <i class="fas fa-table"></i>
                            <span>Appointments</span>
                        </a>
                        <div id="appointmentDropdown" class="collapse" aria-labelledby="headingAppointment" data-bs-parent="#accordionSidebar">
                            <div class="bg-white py-2 collapse-inner">
                                <a class="collapse-item" href="/display/appointment">Pending Requests</a>
                                <a class="collapse-item active" href="/display/approved">Approved Requests</a>
                                <a class="collapse-item" href="/display/archives">Archives</a>
                            </div>
                        </div>
                    </li>
                    <li class="nav-item" id="profile-item">
                        <a class="nav-link" href="/admin/profile.html">
                            <i class="fas fa-user"></i>
                            <span>Profile</span>
                        </a>
                    </li>
                    <li class="nav-item" id="cms-item">
                        <a class="nav-link" href="/admin/cms.html">
                            <i class="fa fa-book"></i>
                            <span>CMS</span>
                        </a>
                    </li>
                </ul>
                
                <div class="text-center d-none d-md-inline">
                    <button class="btn rounded-circle border-0" id="sidebarToggle" type="button"></button>
                </div>
            </div>
        </nav>
        
        <div class="d-flex flex-column" id="content-wrapper">
            <div id="content">
                <nav class="navbar navbar-light navbar-expand shadow mb-4 topbar static-top" style="background: rgb(0,0,0);">
                    <div class="container-fluid">
                        <button class="btn btn-link d-md-none rounded-circle me-3" id="sidebarToggleTop" type="button">
                            <i class="fas fa-bars"></i>
                        </button>

                        <ul class="navbar-nav flex-nowrap ms-auto">

                            <div class="d-none d-sm-block topbar-divider"></div>
                            <li class="nav-item dropdown no-arrow">
                                <div class="nav-item dropdown no-arrow"><a class="dropdown-toggle nav-link" aria-expanded="false" data-bs-toggle="dropdown" href="#"><span class="d-none d-lg-inline me-2 small" style="color: rgb(255,255,255);">Admin Name Here</span><img class="border rounded-circle img-profile" src="/assets/img/avatars/avatar1.jpeg"></a>
                                    <div class="dropdown-menu shadow dropdown-menu-end animated--grow-in">
                                        <a class="dropdown-item" href="/admin/profile.html"><i class="fas fa-user fa-sm fa-fw me-2 text-gray-400"></i>&nbsp;Profile</a>
                                        <div class="dropdown-divider"></div>
                                        <a class="dropdown-item" href="/admin/login.html"><i class="fas fa-sign-out-alt fa-sm fa-fw me-2 text-gray-400"></i>&nbsp;Logout</a>
                                    </div>
                                </div>
                            </li>
                        </ul>
                    </div>
                </nav>

                <!-- Main Content Area -->
                <div class="appointment-container" style="margin: 50px;" id="approvedTable">
                    <div style="display: flex; justify-content: space-between; align-items: center;">
                        <!-- Heading -->
                        <h1 style="color: black;">
                            Appointments || 
                            <span style="font-size: 23px; font-weight: bold; color: rgb(5, 131, 9);">Approved Requests</span>
                        </h1>
                        <!-- Add Button -->
                        <button class="btn btn-primary" style="height: fit-content;" id="addNewCustomerButton" data-bs-toggle="modal" data-bs-target="#customerModal">Add New Customer</button>
                         
                    </div>
                    <table class="table tabler-bordered">
                        <thead style="color: rgb(0, 0, 0);">
                            <tr>
                                <th>Email</th>
                                <th>Phone No.</th>
                                <th>City</th>
                                <th>---</th>
                                <th>Vehicle</th>
                                <th>Car Functionality</th>
                                <th>Actions</th> 
                            </tr>
                        </thead>
                        <tbody id="approvedBody" style="color: black;">
                            <% acceptedAppointments.forEach(appointment => { %>
                                <tr data-id="<%= appointment._id %>">
                                    <td><%= appointment.email %></td>
                                    <td><%= appointment.phonenumber %></td>
                                    <td><%= appointment.city %></td>
                                    <td><%= appointment.platenum %></td>
                                    <td><%= appointment.vehicle %></td>
                                    <td><%= appointment.carfunc %></td>
                                    <td>
                                       <!-- Archive Button -->
                                       <button class="btn btn-danger archive-btn" data-id="<%= appointment._id %>">Archive</button>	
                                    </td>
                                </tr>
                            <% }); %>
                        </tbody>
                           
                    </table>
                </div>

               
<footer class="bg-white sticky-footer">
    <div class="container my-auto">
        <div class="text-center my-auto copyright"><span>Copyright © Logo 2024</span></div>
    </div>
</footer>
</div><a class="border rounded d-inline scroll-to-top" href="#page-top"><i class="fas fa-angle-up"></i></a>
</div>



<div class="modal fade" id="customerModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header" style="background-color: darkred; color: white;" >
                <h5 class="modal-title" >Add New Customer</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" style="background-color: rgb(255, 255, 255);"></button>
            </div>
            <div class="modal-body">
                <form id="vehicleForm" enctype="multipart/form-data">
                    <div class="row">
                        <div class="col-md-6">
                            <label for="name">Customer Name:</label>
                            <input type="text" placeholder="Enter name here.." name="name" id="name" required>
                        </div>
                        <div class="col-md-6">
                            <label for="phone">Phone #:</label>
                            <input type="text" placeholder="+63" name="phone" id="phone" required>
                        </div>
                        <div class="col-md-12">
                            <label for="email">Email:</label>
                            <input type="email" class="form-control" placeholder="example@gmail.com" name="email" id="email" required>
                        </div>
                        <div class="col-md-12">
                            <label for="city">City/Address:</label>
                            <input type="text" class="form-control" placeholder="Marikina City" name="city" id="city" required>
                        </div>
                        <div class="col-md-12">
                            <label for="vehicle">Vehicle:</label>
                            <input type="text" class="form-control" placeholder="Toyota Avanza (2022) -- Black - Automatic - ABC 1234" name="vehicle" id="vehicle" required>
                        </div>
                        <div class="col-md-6">
                            <label for="checkInTime">Check In Time:</label>
                            <input type="text" name="checkInTime" id="checkInTime" placeholder="MM/DD/YYYY HH:MM" required>
                        </div>
                        <div class="col-md-6">
                            <label>Is the car functional?</label><br>
                            <label class="ms-3 me-5" for="yesBtn" style="color: black;">
                                <input type="radio" id="yesBtn" name="carFunctionality" value="yes"> Yes
                            </label>
                            <label for="noBtn" style="color: black;">
                                <input type="radio" id="noBtn" name="carFunctionality" value="no"> No
                            </label>
                        </div>
                        <div class="col-md-12">
                            <label for="services">Services:</label><br>
                            <textarea id="services" name="services" rows="4" class="form-control" placeholder="Enter your suggestions or comments here..."></textarea>
                        </div>
                        <div class="col-md-6">
                            <label for="vehicleImage" class="mb-3">Vehicle Image (optional):</label>
                            <input type="file" accept="image/*" name="vehicleImage" id="vehicleImage" class="form-control" onchange="previewImage(event)">
                            <div id="imagePreview" class="image-preview">
                                <img id="preview" src="#" alt="Image Preview" style="display: none;">
                            </div>
                            <button type="button" class="btn btn-secondary btn-sm mt-2" id="removeImageButton" onclick="removeImage()" style="display: none;">
                                Remove Image
                            </button>
                        </div>
                    </div>
                </form>                
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-primary" id="saveVehicleButton" style="background-color: rgb(0, 0, 0); color: white;">Add Customer</button>
            </div>
        </div>
    </div>
</div>

<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script src="/admin/assets/bootstrap/js/bootstrap.min.js"></script>
<script src="/admin/assets/js/bs-init.js"></script>
<script src="/admin/assets/js/theme.js"></script>
    <script>
        $(document).ready(function () {
    // Archive Button Click Handler for Appointments
    $('.archive-btn').on('click', function () {
        const appointmentId = $(this).data('id');

        if (confirm("Are you sure you want to archive this appointment?")) {
            $.ajax({
                url: `/appointments/${appointmentId}/archive`,
                type: 'PATCH',
                success: function (response) {
                    alert(response.message);
                    location.reload(); // Reload to reflect archived appointments
                },
                error: function (error) {
                    console.error("Error archiving appointment:", error);
                    alert("Failed to archive the appointment. Please try again.");
                }
            });
        }
    });
});

    // Archive Button Click Handler for Customers
$('#approvedBody').on('click', '.archive-btn', function () {
    const customerId = $(this).data('id');

    if (confirm("Are you sure you want to archive this customer?")) {
        $.ajax({
            url: `/customer/${customerId}/archive`,
            type: 'PATCH',
            success: function (response) {
                alert(response.message);

                // Remove the archived customer row dynamically
                $(`tr[data-id="${customerId}"]`).remove();
            },
            error: function (error) {
                console.error("Error archiving customer:", error);
                alert("Failed to archive the customer. Please try again.");
            }
        });
    }
});

    // Save Vehicle Button Handler
    $('#saveVehicleButton').on('click', function () {
        const formData = new FormData($('#vehicleForm')[0]); // Use FormData for file uploads

        $.ajax({
            url: '/Newcustomers', // Backend POST route
            type: 'POST',
            data: formData,
            contentType: false, // Required for FormData
            processData: false, // Prevent jQuery from processing data
            success: function (response) {
                if (response.success) {
                    alert('Customer added successfully!');

                    $('#customerModal').modal('hide'); // Close the modal
                } else {
                    alert(`Error: ${response.message}`);
                }
            },
            error: function (error) {
                console.error('Error:', error);
                alert('Failed to add customer. Please try again.');
            }
        });
    });


    
 // Function to preview the selected image
 function previewImage(event) {
            const preview = document.getElementById('preview');
            const file = event.target.files[0];
            const reader = new FileReader();
            const removeButton = document.getElementById('removeImageButton');

            reader.onload = function() {
                preview.src = reader.result;
                preview.style.display = 'block';
                removeButton.style.display = 'inline-block'; // Show Remove Image button
            };

            if (file) {
                reader.readAsDataURL(file);
            } else {
                preview.src = '';
                preview.style.display = 'none';
                removeButton.style.display = 'none'; // Hide Remove Image button if no file
            }
        }

        // Function to remove the selected image
        function removeImage() {
            document.getElementById('vehicleImage').value = ''; // Clear file input
            document.getElementById('preview').src = ''; // Clear preview
            document.getElementById('preview').style.display = 'none'; // Hide preview
            document.getElementById('removeImageButton').style.display = 'none'; // Hide Remove Image button
        }


    //DI MAWAWALA UNG NASA DROPDOWN
document.addEventListener("DOMContentLoaded", function () {
        const dropdown = document.getElementById("appointmentDropdown");

        // Check and set the state on page load
        if (localStorage.getItem("dropdownState") === "open") {
            dropdown.classList.add("show"); // Bootstrap's class to keep it open
        }

        // Save the state when the dropdown toggles
        dropdown.addEventListener("shown.bs.collapse", function () {
            localStorage.setItem("dropdownState", "open");
        });

        dropdown.addEventListener("hidden.bs.collapse", function () {
            localStorage.setItem("dropdownState", "closed");
        });
    });


    //MAG HIDE NG DROPDOWN IF NAGCLICK OUTSIDE
    document.addEventListener("DOMContentLoaded", function() {
    const dropdownElement = document.getElementById("appointmentDropdown");
    const appointmentDropdown = new bootstrap.Collapse(dropdownElement, {
        toggle: false
    });
    
    // Optional: Add listener to close dropdown when clicked outside
    document.addEventListener("click", (event) => {
        if (!dropdownElement.contains(event.target) && !event.target.matches('[data-bs-toggle="collapse"]')) {
            appointmentDropdown.hide();
        }
        });
    });

    </script>
</body>

</html>
